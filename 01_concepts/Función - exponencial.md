#Función

>[!info]
>Es una función que tiene una base constante y su exponente va incrementando con su variable independiente $x$

# Ecuación
$$y = f(x) = a^x$$
- $a$: Es una constante
- $x$: Es nuestra variable independiente